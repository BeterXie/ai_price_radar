# Release Notes - v3.7.93

**发布日期**: 2026-09-19  
**发布版本**: `v3.7.93`

---

## 变更概述

v3.7.93 全面优化了店铺优惠券的商家归属机制与用户端呈现，去除了优惠券导入与展示中的默认店铺（“彩头AI / wzyp.cn”）硬编码，要求所有优惠券必须显式绑定真实入驻店铺；并在用户界面中增加平台中立性合规说明。

---

## 核心变更

### 1. 优惠券强制绑定真实店铺
- **模型与接口去硬编码**:
  - `ShopCoupon` 模型中的 `shop_name` 与 `shop_url` 默认值均置空，杜绝静默落入全站默认商铺；
  - `AdminCouponImportRequest` 将店铺相关字段设为必填项；
- **管理端导入防错**:
  - 优惠券批量导入弹窗中「归属店铺」设为 `required` 必选下拉框，且不设任何默认店铺，防止管理员漏选。

### 2. 用户中心卡包与彩蛋弹窗优化
- **专属店铺标题行**: 个人中心「我的优惠券」卡片上方增加「适用店铺」独立展示区，突出优惠券的使用场景；
- **防误跳保护**: 对未提供有效 `shop_url` 的优惠券，按钮置灰显示“店铺链接未配置”，杜绝误导用户；
- **中立声明**: 彩蛋掉落浮窗增加合规文案（“优惠券由对应店铺提供，不影响 PriceMemo 的报价排序”），避免影响比价中立性认知。

---

## 升级与迁移说明

- 本次发布无数据库结构变更，无迁移脚本；
- 遵循 `docs/QUICK_DEPLOY.md` 生产标准部署：
  - 生产重建 `api`、`web` 容器。
