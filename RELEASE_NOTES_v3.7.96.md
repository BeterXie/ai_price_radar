# Release Notes - v3.7.96

**发布日期**: 2026-09-21
**发布版本**: 3.7.96

---

## 变更概述

v3.7.96 收口上一轮安全评审与产品化改造，并修复价格历史接口在大规模 `offer_history` 数据下全量加载 ORM 对象导致 API 进程内存暴增的问题。

## 核心变更

### 1. 安全与运行时可靠性

- 完成认证、机器人凭据加密、隐私清理、通知 Outbox、来源收录、公开 URL、分页边界和运行时配置校验等安全修复。
- 强化生产环境预检：应用模式、邮件传输、公开站点地址、支持信息和独立密钥必须符合生产要求。
- 补充 SQLite/PostgreSQL 兼容迁移与回归测试。

### 2. 价格历史 OOM 修复

- `/api/v1/products/{slug}/history` 改为数据库端按天、交付形态和价格分组聚合。
- Python 端只处理有界的价格直方图，通过加权中位数保持原有可信最低价和中位价口径。
- 增加按商品、币种、来源平台、时间范围和当前快照隔离的 TTL/LRU 缓存；`PRICE_TREND_CACHE_TTL_SECONDS=0` 可关闭缓存。

### 3. Web 与运营能力

- 完善商品目录筛选、分页、来源发现/收录、店铺绑定优惠券、公开 Feed、指南、错误页、监控与后台交互。
- 增加 CI、Compose、发布前预检和生产回滚保护。

## 升级与迁移说明

- 本版本没有新增独立迁移脚本；API 启动时的幂等 schema 兼容逻辑会处理现有 SQLite/PostgreSQL 表的增量字段与索引。
- 生产发布必须使用本版本 Tag，并按照 `docs/QUICK_DEPLOY.md` 完成备份、staging 预检、镜像切换和完整验收。
