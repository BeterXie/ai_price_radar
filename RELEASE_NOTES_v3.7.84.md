# Release Notes - v3.7.84

## 概述 (Overview)

v3.7.84 优化并落地了**店铺专属优惠券绑定体系（券跟着店铺走）**。针对链动小铺（LDXP）及各类商户优惠券天然归属单一商户的业务现实，系统全面实现了优惠券、营销活动口令与平台具体店铺（Shop）的强绑定与隔离。

主要变更包括：
1. **数据模型与迁移**：
   - `shop_coupons` 增加 `shop_id` 外键与索引；
   - `coupon_campaigns` 增加 `shop_id`、`shop_url`、`shop_name` 字段与索引；
   - 数据库双引擎迁移脚本 `scripts/migrate_coupon_shop_binding_v19.py`，支持 PostgreSQL 和 SQLite，并自动回填存量数据。
2. **商户活动隔离**：
   - 营销活动口令（Campaign）必须绑定指定店铺；
   - 口令兑换（`/api/v1/user/coupons/redeem`）仅从该店铺的专享可用券池中发放，不会跨店铺发放或挪用其他商家的优惠券；
   - 掉落通知动态显示券归属的店铺名。
3. **管理后台交互**：
   - 券码批量导入：支持选择已有平台店铺或录入店铺直达链接，自动匹配关联平台店铺；
   - 链动小铺同步：自动绑定彩头AI官方店铺；
   - 营销口令新建：增加店铺选择器与自定义店铺录入；
   - 库存明细与活动列表：展示店铺徽章，并支持按店铺筛选券码库存。
