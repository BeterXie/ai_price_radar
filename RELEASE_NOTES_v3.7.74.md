# Release Notes - v3.7.74

## 变更摘要

1. **移除开发调试痕迹**:
   - 彻底移除了扫码绑定界面中的 `🧪 模拟手机扫码成功（开发调试）` 按钮，防止线上暴露调试操作。
2. **完全对齐 PriceMemo 纸质报刊设计系统规范**:
   - 移除原先遗留的 Dark/Zinc 突兀暗色卡片样式；
   - 登录弹窗与个人中心全面采用项目原生设计语言（`var(--panel)`、`var(--paper)`、`var(--ink)`、`var(--line)`、`.surface-panel`、`.button-primary`、`.tactile` 等）；
   - 手机 QQ 扫码卡片采用极简米白底色、清晰居中二维码、柔和呼吸指示灯与优雅的折叠备选指引，浑然一体。
