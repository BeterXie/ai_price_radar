# Release Notes - v3.7.83

**发布日期**: 2026-09-17
**发布目标**: 修复活动口令（营销暗号）兑换逻辑、批次匹配容错与各活动配额独立隔离。

---

## 变更明细

### 1. 活动口令限领配额精准隔离
- `ShopCoupon` 数据表新增 `campaign_id` 外键与 `(assigned_user_id, campaign_id)` 联合索引；
- 解决之前按用户总持券数判断导致已拥有通用券或掉落券的用户被误判为“已参与过该活动”的问题；
- 用户参与不同营销口令活动及前台幸运掉落互不影响，各自独立校验 `max_per_user`。

### 2. 券批次库存自动兜底分配机制
- 当活动口令指定了特定批次（`coupon_batch_id > 0`）但该批次无可用库存或误填了不存在的批次号时，系统自动兜底从通用可用券池中进行分配，彻底消除“优惠券库存暂时不足”的非预期阻断。

### 3. 后台管理表单优化
- 优化活动口令创建弹窗中的批次 ID 标签与占位说明，明确指导无需绑定特定批次时留空或保持 0 即可。
