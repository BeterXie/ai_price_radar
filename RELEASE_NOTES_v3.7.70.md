# AI Price Radar v3.7.70 — Group Shop Offers & Snapshot Fallback Fix

## 概述
本版本针对商品详情卡片展开后“全部店铺报价”偶现空白框的缺陷进行了根因分析与前后端协同加固。

## 根因剖析
1. **快照接力机制导致旧快照作废**：
   - 数据库中的 `offers` 表为实时业务表，后台爬虫在持续写入并发布新快照时，通过 `_carry_forward_current_snapshot` 将上一批活跃报价的 `snapshot_id` 直接批量更新为最新的目标快照 ID。
   - 客户端页面由 Next.js ISR/SSR 渲染或在浏览器停留一段时间后，客户端持有的 `snapshot_id` 为较早的版本（如 4480）。
   - 用户展开同款商品卡片时，前端向接口请求 `GET /api/v1/products/.../groups/.../?snapshot=4480`。
   - 后端在 `offers` 表中查询 `Offer.snapshot_id == 4480`，此时所有活跃报价已全部更新为最新快照（如 4483），匹配结果为 0 条，接口返回 `{"items": []}`。
2. **前端空列表边界渲染漏洞**：
   - 前端 `ShopOfferList` 在接收到空数组时，直接渲染了一个空的带有边框与内边距的容器 `div`，未展示任何店铺报价或空态提示，给用户造成“空白 Bug”的视觉体验。

## 修复措施
1. **后端快照回退与同款保底（`apps/api/app/services/catalog.py`）**：
   - `_snapshot_for_query` 增加有效报价检测：当客户端请求的历史快照在数据库中已无有效报价时，自动降级至当前最新快照，彻底避免过期快照导致全站数据置空。
   - `get_group_offers` 增加同款店铺查询保底：若在严格过滤条件下同款店铺查询结果为空，自动以当前快照查询该款式的全量店铺报价，确保展开卡片时必能看到该款式来源各店铺对比。
2. **前端查询优化与多重兜底（`apps/web/components/offer-table.tsx`）**：
   - 在展开查看具体商品款式下的各店铺报价时，不再强行拼接过期的 snapshot 参数。
   - 接口返回为空或网络异常时，前端自动降级渲染当前卡片已有的代表店铺报价（`[offer]`），确保始终有店铺名称、价格、库存和直达链接。
   - `ShopOfferList` 针对空数据增加防御处理，彻底消除空边框容器。
