# Release Notes - v3.9.1

**发布日期**: 2026-09-24  
**发布版本**: 3.9.1

## 变更概述

v3.9.1 优化全站顶部导航栏的排版布局与信息层次，解决宽屏与窄屏下的挤压折行问题：

1. **精简顶部主导航栏**：
   - 将顶部中央导航收敛为 5 个核心顶级频道：`报价目录`、`中转站`（独立专区）、`技能与实验室`、`购买指南`、`数据方法`。
   - 移除桌面端主导航条中平铺的商品品牌过滤标签（OpenAI、Claude、Gemini、Grok 等），避免与下方 Hero 区域搜索框底部的「常用入口」产生视觉竞争与信息冗余。
   - 彻底解决导航容器在不同分辨率下被撑爆、中文字断行（如“报价日/录”、“中转/站”）的低级排版问题。
2. **样式加固**：
   - 为 `.nav-link` 强制增加 `white-space: nowrap;`，杜绝任何字号或缩放下的意外汉字截断与断行。
3. **移动端抽屉菜单梳理**：
   - 移动端顶部保持与桌面端一致的一级频道列表；
   - 移动端底部“按品牌查看报价”网格保留纯粹的模型品牌，结构层次更加统一规范。

## 部署说明

- 无数据库变更，无数据库结构迁移操作。
- 遵循 `docs/QUICK_DEPLOY.md`，使用本机生成的 standalone Web 包部署并重启 `web` 容器，同时更新 `api` 容器以匹配发布版本。
