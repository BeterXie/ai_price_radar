# Release Notes - v3.9.0

**发布日期**: 2026-09-24  
**发布版本**: 3.9.0

## 变更概述

v3.9.0 新增后台可配置的广告栏位与「中转站」平台专区，下线智谱品牌，并修复商品页偶发“没有一条报价”的问题：

1. **广告栏位（后台配置）**：
   - 新增 `ad_slots` 表与 `GET /api/v1/ads?placement=…`、`POST /api/v1/ads/{id}/click` 公开接口，`/api/v1/admin/ads` 管理接口（增删改查）。
   - 栏位：首页概览下方（`home_hero`）、报价目录顶部（`catalog_top`）、商品页报价列表上方（`product_offers`，紧凑样式）、中转站专区顶部（`relay_hub`）。
   - 支持排序、上下线时间、赞助方名称、角标与按钮文案；前台一律带「广告 / 赞助」标识，外链使用 `rel="sponsored nofollow"`。
   - 跳转与图片链接只接受站内路径或公开 HTTPS 地址；历史不安全值在读取时被清空而不是泄露到页面。
   - 全局开关 `ad_slots_enabled`（后台「广告与中转站」标签页）。
2. **中转站平台**：
   - 新增 `relay_stations` 表、`GET /api/v1/relays`、`POST /api/v1/relays/{id}/click` 与 `/api/v1/admin/relays` 管理接口。
   - 新增 `/relays` 页面；导航栏、品牌筛选栏与首页常用入口新增「中转站」入口；`/meta` 返回 `relay_hub_enabled` 与 `relay_station_count`。
   - 字段：名称、HTTPS 地址、一句话简介、详细说明、支持模型、价格说明、计费方式、特性标签、赞助置顶、排序、上线状态。
   - 中转站不进入标准产品报价统计；专区页面明确“收录不等于推荐或担保”。
3. **下线智谱品牌**：
   - 从品牌标签、演示数据、API 与 pipeline 分类器、机器人指令中移除；`ensure_db_schema` 与 `ensure_products` 会把历史 `zhipu-*` 标准产品置为不可见，不删除数据。
4. **商品页空报价修复**：
   - 根因：商品卡片统计全部公开报价，而商品工作区默认只看“可直接比较”报价；当某商品的报价全部是号池 / 中转 / 体验号等形态时，点开后一条都不显示。
   - 现在在访客未显式选择比较范围、且没有其他筛选条件时，自动扩大到全部相关报价并给出说明（可一键切回“只看可直接比较”）。
   - 修复“包含相关商品”实际向 API 发送 `comparable=false`（只返回不可比较报价）的问题，现改为不传该参数即返回全部报价。

## 部署说明

- 新表 `ad_slots`、`relay_stations` 由 API 启动时的 `Base.metadata.create_all` 自动创建，无需单独迁移脚本。
- API 启动会自动隐藏 `zhipu-*` 标准产品；下一次完整目录发布也会保持隐藏。
- 按 `docs/QUICK_DEPLOY.md` 规范重新构建 `api`、`web`、`importer` 与 `crawler`。
- 发布后在后台「广告与中转站」标签页配置中转站与广告栏位；未配置时前台不显示任何栏位，中转站页面显示空状态。
