# Release Notes - v3.9.2

**发布日期**: 2026-09-24  
**发布版本**: 3.9.2

## 变更概述

v3.9.2 优化报价目录中“中转站”品牌的排版一致性，并大幅优化移动端筛选 Tab 栏的自动换行与缩进排版：

1. **“中转站”品牌目录行为收敛与排版统一**：
   - 之前主目录品牌栏点击“中转站”会硬编码跳转到独立页面 `/relays`，造成三层筛选栏、已选条件条、统计概况和报价列表完全消失。
   - 本次将“中转站”统一为标准品牌筛选（`/?brand=中转站`），底层自动映射公开店铺抓取的 API 中转/反代商品（`delivery_type=relay_api`），完整保留品牌选择、商品类型、来源平台、已选条件与聚合报价表格。
   - 在筛选区下方保留专区入口指引横幅，兼顾完整目录体验与独立中转站专区导航。

2. **移动端 Tab 自动换行与结构化缩进**：
   - 解决移动端原生滚动条横向截断、滑动生硬的问题；
   - 移动端引入结构化换行（`.filter-group` 与 `.filter-options`）：最左侧固定展示全局项（“全部”、“全部商品”、“全部来源”）；
   - 右侧具体品牌/类型折行到第二排时，自动向后缩进对齐（对齐到第二列），不再落在“全部”正下方，彻底避免与“全部”列产生上下挤占重叠，整体视觉清爽规整。

3. **桌面端零回归保证**：
   - 桌面端（`> 768px`）采用 `display: contents;`，容器扁平化展平，保持原有单行横向排版 100% 不受影响。

## 部署说明

- 无数据库结构变更，无迁移操作。
- 遵循 `docs/QUICK_DEPLOY.md`，使用本机生成的 standalone Web 包部署并重建 `api`、`source-detector`、`web` 与 `importer` 容器。
