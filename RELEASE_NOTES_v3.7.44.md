# Release Notes v3.7.44

## 变更摘要

- **前台全面显式展示商品报价 ID (`#ID`)**：
  - 在前台商品列表卡片次级标题栏（`offer-table.tsx`）增加高可读性的等宽 monospace 报价编号徽标：`#{offer.id}`。
  - 在商品卡片展开的决策详情（`DecisionFacts`）中新增【报价编号：`#{offer.id}`】指标。
  - 在店铺全部报价折叠列表（`ShopOfferList`）中展示报价编号，方便直接比对核查。

- **后台管理报价默认对齐当前在售快照（解决前后台商品与库存不同步问题）**：
  - `GET /api/v1/admin/offers` 接口新增 `scope` 参数（`current` / `all`），默认限制为当前在售快照（`scope="current"`）。
  - 过去商家在几周或几个月前下架/删除的商品（历史快照冻结在有货状态）不再混入后台在售列表，前后台列表与库存状态完全一致。
  - 后台统计概览（`GET /api/v1/admin/stats`）中的公开报价总数、各品牌/产品商品数全面对齐当前在售快照。

- **后台管理搜索支持商品报价 ID（`#ID` 或数字 ID）直达**：
  - 搜索框支持直接输入带有 `#` 的 ID（如 `#10194`）或纯数字（如 `10194`），精准定位指定报价。
  - 显式 ID 搜索支持自动穿透历史快照，无论商品是否在当前快照中均可快速定位。
  - 优化搜索框提示占位符：`按标题、店铺名称、拦截原因或 #报价ID 搜索...`。

- **后台工具栏增加快照范围筛选下拉框**：
  - 新增快照范围下拉选择：`范围：当前在售快照 (默认)` / `范围：全部历史记录`，方便管理员在排查问题时查看完整历史入库报价。
