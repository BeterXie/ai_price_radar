# Release Notes - v3.8.2

**发布日期**: 2026-09-23  
**发布版本**: 3.8.2

## 变更概述

v3.8.2 包含顶部网站公告外链按钮恢复与会话级展示逻辑优化，以及官方移动商城邮箱验证服务对接：

1. **顶部公告外链按钮恢复**：
   - 修复 `SiteNoticePrompt` 链接解析逻辑，新增 `safeNoticeLink` 工具函数，同时兼容站内相对路径与安全的外部 HTTPS 链接（如 QQ 交流群 `https://qm.qq.com/...`）。
   - 修复因历史严格站内路径校验导致外部跳转按钮（如“加入QQ群”）不渲染的问题。
2. **公告展示与关闭体验优化**：
   - 将公告关闭状态存储介质由 `localStorage`（永久存储）调整为 `sessionStorage`（会话级存储）。
   - 用户在浏览过程中关闭公告后，在当前会话各页面跳转均保持关闭；当关闭网页/标签页重新打开网站时，若后台开启公告，将重新展示最新公告。
   - 自动清理历史版本留在 `localStorage` 中的永久关闭标识，确保所有用户均能按预期看到开启的公告。
3. **官方移动商城服务对接**：
   - 移动商城（`shop.pricememo.cn`）支持场景化邮箱验证码申请与核验，对接系统通知 outbox 队列发送服务开通凭证。

## 部署说明

- 本版本无数据库结构迁移要求。
- 按 `docs/QUICK_DEPLOY.md` 规范构建并部署 `api` 与 `web` 容器。
