# Release Notes - v3.7.91

**发布日期**: 2026-09-18  
**发布版本**: `v3.7.91`

---

## 变更概述

v3.7.91 实现了前台优惠券彩蛋触发次数统计、彩蛋防刷门槛升级（非首页真实点击门槛）以及每分钟至多判定一次的频控保护，同时包含了生产服务资源配额与调度周期调优。

---

## 核心变更

### 1. 前台优惠券彩蛋触发次数统计与展示
- **后端持久化与上报**:
  - 新增 `POST /api/v1/user/coupons/record-drop-trigger` 接口，支持匿名访客与已登录用户上报彩蛋触发事件；
  - 写入 `UserActionLog`（`action_type="coupon_drop_trigger"`）记录客户端 IP、User-Agent、触发页面与时间；
  - `SystemSetting(key="coupon_drop_trigger_count")` 原子持久化维护全局累计触发数；
- **管理后台指标呈现**:
  - `AdminCouponStats` 新增 `drop_trigger_count` 字段；
  - 优惠券管理后台概览卡片新增「彩蛋触发次数」卡片，与可掉落库存、已发放/领取、已核销使用形成清晰的彩蛋转化漏斗。

### 2. 彩蛋概率触发门槛升级与防刷频控
- **排除首页**: 首页（`/`）不进行彩蛋倒计时与触发判定，避免纯进首页挂机或爬虫自动触发；
- **非首页真实点击门槛**: 仅在用户于非首页页面（商品比价、分类等）产生真实有效点击（`e.isTrusted`）后，才开始进入概率判定流程；
- **频控限制**: 限制每分钟至多触发一次概率判定（`EVAL_COOLDOWN_MS = 60_000`），避免高频切页刷取。

### 3. 生产服务资源与调度调优
- 调整 `ai-price-radar-inventory` 定时周期为 20 分钟并配置 CPU/内存限额；
- 优化 Caddy 代理重试与反向代理连接池保活策略。

---

## 升级与迁移说明

- 本次发布无数据库结构迁移（利用已有的 `user_action_logs` 与 `system_settings` 表）；
- 遵循 `docs/QUICK_DEPLOY.md` 生产标准部署：
  - 生产重建 `api`、`web` 容器。
