# Release Notes v3.7.45

## 变更摘要

- **前台分类与目录商品报价排序优化（在售商品严格按价格升序靠前展示）**：
  - 修复此前商品列表排序中，因异常低价警告（低于同交付类型中位价 40%）将合法低价在售商品（如 ¥23.00、¥26.45、¥27.60 等短租/日抛/特惠账号）强制沉底降级至页面末尾的问题。
  - 统一 `_offer_sort_key` 与 `_trusted_offer_sort_key` 排序规则：
    1. **在售状态优先**（有货在前，缺货在后）；
    2. **标准可比优先**（标准商品在前，非标品/教程在后）；
    3. **人民币优先**（CNY 优先）；
    4. **正常价格优先**（有效价格 >= 1 元在前，0.01 元等测试/补差价微额占位链接自动沉底至最后）；
    5. **价格从低到高优先**（¥23.00 -> ¥26.45 -> ¥27.60 -> ¥28.00 -> ¥39.68 -> ... 真正实现低价优先与靠前展示）；
    6. **最新采集时间优先**。
  - 优化报价分组 `lowest_price` 取值逻辑：当某组报价全部触发低价提醒时，自动平滑回退至在售有效报价最低价，确保前台卡片价格与分组价格准确反映实盘低价。
  - 保留并醒目展示 `● 价格明显偏离同类报价` 风险警示胶囊，在保证买家能第一时间看到超值特惠商品的同时，清晰提示注意核对使用期限与质保类型。
